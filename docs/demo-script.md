# 45-second Demo Script

1. Show `docker compose up --build`
2. Open `GET /health` -> `{ ok: true }`
3. Send sample webhook via curl
4. Show `data/leads.csv` updated
5. Open `GET /stats` and show lead counter
6. Show manager Telegram alert screenshot (optional)

Closing line:
"MVP delivered in 48 hours, ready for CRM integration and multilingual flows."
